{
  "name": "echomedia",
  "version": "1.0.0",
  "description": "Echo media file delivery service",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "build": "node scripts/write-build-info.mjs src/build-info.json",
    "test:version": "node --test scripts/check-build-info.mjs",
    "test": "npm run test:version && node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  }
}
